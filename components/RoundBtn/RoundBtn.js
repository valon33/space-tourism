import React from "react";
import styles from "./RoundBtn.module.css";

const RoundBtn = () => {
  return (
    <div className={styles.Round}>
      <span className={styles.RoundText}>Explore</span>
    </div>
  );
};

export default RoundBtn;
